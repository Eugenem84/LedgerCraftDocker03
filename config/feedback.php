<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Обратная связь: отчёты «Сообщить об ошибке» (Фаза 14, решение D7)
    |--------------------------------------------------------------------------
    |
    | `pull_token` — отдельный токен выгрузки отчётов для разработки/агента. Он НЕ
    | пользовательский: маршрут `GET /api/feedback` не требует `auth:sanctum` и
    | отдаёт отчёты только по заголовку `X-Feedback-Token`. Пустой токен = выгрузка
    | выключена (503), чтобы не открыть отчёты случайно.
    |
    | `rate_limit_per_hour` — сколько отчётов один пользователь может отправить за
    | час (`POST /api/feedback` под `throttle`): кнопка ручная, отчётов мало, всё
    | сверх лимита — это петля или злоупотребление.
    |
    | `export_directory` — куда пишет дайджест команда `php artisan feedback:export`.
    |
    */

    'pull_token' => env('FEEDBACK_PULL_TOKEN', ''),

    'rate_limit_per_hour' => (int) env('FEEDBACK_RATE_LIMIT_PER_HOUR', 10),

    'export_directory' => env('FEEDBACK_EXPORT_DIR', storage_path('app/feedback')),

];
